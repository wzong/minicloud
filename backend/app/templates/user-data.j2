#cloud-config
hostname: {{ hostname }}
manage_etc_hosts: true
users:
  - name: ubuntu
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    ssh_authorized_keys:
      - {{ ssh_public_key }}
packages:
  - qemu-guest-agent
package_update: true
runcmd:
  - systemctl enable --now qemu-guest-agent
